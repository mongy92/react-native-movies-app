import { StyleSheet } from 'react-native';
import { COLORS } from '../common';

export default StyleSheet.create({
  headerTitleStyle: {
    color: COLORS.black,
    fontSize: 20,
    fontWeight: 'bold',
  },
});
